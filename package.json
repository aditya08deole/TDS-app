{
  "name": "evaratds-unified",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm install && cd admin_dashboard && npm install && cd ../backend && npm install",
    "build": "cd admin_dashboard && npm install && npm run build && cd ../backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "db:init": "cd backend && npm run db:init"
  }
}
