rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write for authenticated users
      // In production, you should tighten this with role-based checks
      allow read, write: if request.auth != null;
    }
  }
}
