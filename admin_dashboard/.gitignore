# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
# .env.production has no secrets (just the public Railway API URL) and is
# committed on purpose — it's what pins production builds against
# .env.local overrides that only make sense on a specific dev machine
# (see .env.production's own header comment for the full story).
!.env.production

# Firebase
.firebase/
firebase-debug.log
functions/firebase-debug.log

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

android/app/google-services.json
android/app/build/
android/.gradle/
android/local.properties
